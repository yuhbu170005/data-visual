/**
 * barChart.js — Bar chart placeholder
 *
 * Implement a reusable D3 bar chart here.
 *
 * Suggested use cases for NYC hotel data:
 *   - Average nightly price per borough
 *   - Number of hotels by room type
 *   - Availability count per neighbourhood
 *
 * Recommended pattern:
 *   export function initBarChart(selector, data, options = {}) { ... }
 */

// TODO: implement bar chart
