/**
 * choroplethMap.js — Choropleth map placeholder
 *
 * Implement a D3 choropleth (geographic heat map) here.
 *
 * Suggested use cases for NYC hotel data:
 *   - Average price density by neighbourhood
 *   - Hotel count per borough overlaid on a NYC GeoJSON boundary file
 *
 * Dependencies:
 *   - d3-geo (included in the full D3 bundle)
 *   - NYC borough / neighbourhood GeoJSON (place in public/data/)
 *
 * Recommended pattern:
 *   export function initChoroplethMap(selector, geoData, hotelData, options = {}) { ... }
 */

// TODO: implement choropleth map
