/**
 * lineChart.js — Line chart placeholder
 *
 * Implement a reusable D3 line chart here.
 *
 * Suggested use cases for NYC hotel data:
 *   - Price trends over time (if temporal data is available)
 *   - Availability trends by month
 *
 * Recommended pattern:
 *   export function initLineChart(selector, data, options = {}) { ... }
 */

// TODO: implement line chart
